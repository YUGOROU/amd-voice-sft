name: Sync to HF Space

on:
  push:
    branches: [main, feature/integrated-lumi]
    paths: ["hf_upload/**"]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install huggingface_hub
        run: pip install --quiet huggingface_hub

      - name: Push hf_upload/ to HF Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python - <<'EOF'
          import os
          from huggingface_hub import HfApi

          api = HfApi(token=os.environ["HF_TOKEN"])
          api.upload_folder(
              folder_path="hf_upload",
              repo_id="Debdeep30/lumi-ai-voice-companion",
              repo_type="space",
              commit_message=f"Sync from GitHub @ ${{ github.sha }}",
          )
          print("Synced hf_upload/ → HF Space")
          EOF
